# Tells Grafana to load dashboard JSON from /var/lib/grafana/dashboards.
apiVersion: 1

providers:
  - name: Cohesiq
    orgId: 1
    folder: Cohesiq
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
